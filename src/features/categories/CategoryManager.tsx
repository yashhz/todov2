// CategoryManager is superseded by the Projects system.
// This file is kept as a stub to avoid import errors from any lingering references.
export default function CategoryManager({ onClose }: { onClose: () => void }) {
    onClose();
    return null;
}
