## 2024-06-01 - Initial Setup
**Learning:** Found several inline clear buttons (×) without aria-labels across Habits.tsx and Tasks.tsx components.
**Action:** Always verify that inline dismiss/clear buttons have explicit `aria-label` and `title` to ensure accessibility.
